<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.com.desoft.hidrogas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"
            android:label="Register New Account"></activity>

        <activity android:name=".AdministradorPipas"
            android:label="Register New Account"></activity>

        <activity android:name=".AgregarEditarPersonal"
            android:label="Register New Account"></activity>

        <activity android:name=".AgregarPipas"
            android:label="Register New Account"></activity>

        <activity android:name=".LiquidacionUnidades"
            android:label="Register New Account"></activity>

        <activity android:name=".ListaLlenadoPipas"
            android:label="Register New Account"></activity>

        <activity android:name=".ListaPersonal"
            android:label="Register New Account"></activity>

        <activity android:name=".LlenarPipa"
        android:label="Register New Account"></activity>

        <activity android:name=".MainActivity"
            android:label="Sistema De Control y Liquidación De Unidades"></activity>
    </application>

</manifest>